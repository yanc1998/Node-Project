# Node-Project
para aprender a desarrollar en node

Esto es pa jose
el objeto paginate debe contener los siguientes campos :
paginate = {
    filter: {},
    page: num,
    limit: num
}

paginateReturn = {
    total:num,
    page:num,
    pages:num
    items: []
}
